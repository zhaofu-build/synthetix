# Synthetix 前端交互与 UI 优化报告

> 基于对主页面（UnifiedEditor）、工作面板（WorkspacePanel）、AI 对话栏（ChatSidebar）、素材库（MaterialsPanel）、视频预览（PreviewPanel）和顶部菜单栏（MainLayout）的全面分析。

---

## 一、欢迎页（无项目时）

### 现状问题
1. **空白感强** — 仅一个标题 + 按钮 + 表格，缺少品牌调性和引导性
2. **无新手引导** — 新用户进入后不知道能做什么，没有示例或教程
3. **项目列表交互单一** — 仅文字表格，缺少封面缩略图、最近编辑高亮
4. **创建流程过简** — 只有名称和描述，缺少模板/类型选择

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **品牌视觉增强** | 添加产品插画或动画背景，配合品牌色渐变，营造专业工具氛围 | P2 |
| **快捷入口卡片** | 在创建按钮下方增加 3-4 个快捷场景卡片：「30s 短视频」「Vlog 剪辑」「产品宣传」「从模板创建」 | P1 |
| **项目卡片视图** | 表格改为卡片网格布局，每张卡片显示：缩略图封面、项目名、状态徽标、最后编辑时间、进度百分比 | P1 |
| **最近项目高亮** | 置顶最近 3 个项目，配合 `今日编辑` / `昨日编辑` 时间分组标签 | P2 |
| **新手引导气泡** | 首次进入时展示 2-3 步操作引导（Step Tour），介绍主要功能区域 | P2 |
| **创建项目增强** | 添加项目类型选择（短视频/长视频/混剪）、目标平台选择（抖音/B站/YouTube）自动匹配时长和比例 | P3 |

---

## 二、顶部菜单栏（MainLayout）

### 现状问题
1. **工具菜单过于拥挤** — 13 个工具项平铺在一级下拉，查找困难
2. **无快捷键提示** — 菜单项旁无快捷键标注（如 Ctrl+S 保存）
3. **项目状态不可见** — 仅显示项目名，无法快速了解项目状态（是否有未保存更改）
4. **面包屑缺失** — 切换项目后无路径反馈，用户容易迷失当前上下文

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **工具菜单分组** | 下拉菜单按「AI 能力」「音视频处理」「系统管理」分组，加分割线和分组标题 | P1 |
| **快捷键标注** | 菜单项右侧显示快捷键（`Ctrl+S` 保存、`Ctrl+N` 新建等） | P2 |
| **项目未保存标识** | 项目名旁显示未保存圆点（`●`），保存后消失 | P1 |
| **BreadCrumb 面包屑** | Logo 后加面包屑：`项目 > [项目名] > 编辑器` | P3 |
| **全局搜索** | 在菜单栏右侧加搜索框，支持搜索项目名、工具、素材 | P3 |
| **最近工具** | 记住最近使用的 3 个工具，在工具菜单顶部快速访问 | P2 |

---

## 三、编辑器主布局（UnifiedEditor）

### 现状问题
1. **面板比例固定** — `4fr:3fr:2fr` 无法调整，用户不能根据工作需要自定义
2. **折叠交互生硬** — 折叠/展开仅一个圆形按钮，无拖拽调整能力
3. **无面板记忆** — 刷新页面后面板状态丢失（虽有 Pinia，但无 localStorage 持久化）
4. **窄屏体验差** — 900px 以下直接堆叠，缺少 tab 切换模式

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **可拖拽分割条** | 面板间添加 4px 拖拽分割条（Resize Handle），支持鼠标拖拽调整宽度，带视觉反馈（高亮线） | P1 |
| **布局预设** | 工具栏提供 3 种布局预设：「专注剪辑」（左大右小）、「AI 对话」（中间大）、「全屏预览」（右全屏） | P2 |
| **面板状态持久化** | 面板折叠状态和比例存入 localStorage，刷新后恢复 | P1 |
| **Tab 模式适配窄屏** | <900px 时切换为顶部 Tab 导航模式（工作区 / 对话 / 素材预览），而非垂直堆叠 | P2 |
| **快捷键切换面板** | `Ctrl+1/2/3` 快速切换面板聚焦，`Ctrl+B` 切换左面板 | P2 |
| **双击面板头折叠** | 双击面板标题栏快速折叠/展开（类似 VS Code） | P3 |

---

## 四、工作面板（WorkspacePanel）

### 现状问题
1. **剪辑方案与音频固定 2:1 比例** — 不可调整，音频需求多时空间不够
2. **剪辑方案区域信息密度低** — 配置项和片段列表各占一半，空状态浪费空间
3. **无时间轴可视化** — 片段列表是纯表格，缺少直观的时间轴/故事板视图
4. **音频面板过于简陋** — TTS 和 BGM 混在一起，无波形预览
5. **无操作历史** — 修改片段后无法撤销

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **可拖拽分割** | 剪辑方案和音频区域间添加可拖拽分割条 | P1 |
| **折叠式配置** | 创意描述、时长、风格等配置项改为可折叠面板，生成方案后自动收起，释放空间给片段列表 | P1 |
| **时间轴/故事板视图** | 片段列表上方添加横向时间轴，每个片段为彩色块，hover 显示详情，支持拖拽排序 | P1 |
| **片段拖拽排序** | 片段支持拖拽调整顺序，拖拽时显示插入位置指示线 | P2 |
| **片段缩略图** — 每个片段在列表中显示视频首帧缩略图（20x14px），便于快速识别 | P2 |
| **音频波形预览** — TTS/BGM 播放时显示简单波形条，增强视觉反馈 | P3 |
| **撤销/重做** — 支持 `Ctrl+Z` / `Ctrl+Y` 撤销重做操作，维护操作栈 | P2 |
| **快捷操作工具栏** — 片段上方显示快捷操作：全选、反选、批量删除、片段合并 | P3 |

---

## 五、AI 对话栏（ChatSidebar）

### 现状问题
1. **输入框固定 2 行** — 长输入体验差，无法自动扩展
2. **消息无时间戳** — 无法区分消息时间
3. **无消息操作** — 不能复制、重新生成、收藏某条消息
4. **快捷操作固定 4 个** — 不根据上下文动态变化
5. **确认面板信息密度高** — JSON 参数直接显示，非技术用户难以理解
6. **无搜索功能** — 历史消息多了后无法查找

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **自适应输入框** — textarea 根据内容自动扩展（2-6 行），超长时内部滚动 | P1 |
| **消息时间戳** — 每条消息右上角显示相对时间（`刚刚`、`5分钟前`），hover 显示完整时间 | P2 |
| **消息操作菜单** — hover 消息时显示操作菜单：复制、重新生成、引用回复 | P2 |
| **上下文快捷操作** — 快捷操作标签根据当前上下文动态生成（如：有素材时显示「分析素材」；无片段时显示「生成方案」） | P1 |
| **确认面板优化** — 将 JSON 参数转为人类可读的表格式展示，关键参数加粗高亮 | P1 |
| **消息搜索** — 顶部添加搜索图标，展开搜索框过滤历史消息 | P3 |
| **打字指示器** — AI 回复前显示打字动画（三点跳动），而非简单的 loading 文字 | P2 |
| **代码块高亮** — AI 返回的代码块支持语法高亮和一键复制 | P2 |
| **流式打字效果** — SSE 流式回复时逐字显示，配合闪烁光标效果，而非整段刷新 | P1 |
| **对话模板** — 在输入框上方提供可展开的提示模板列表（「帮我分析这个视频」→ 自动填入提示词） | P3 |

---

## 六、素材库（MaterialsPanel）

### 现状问题
1. **纯表格展示** — 无缩略图，素材难以直观识别
2. **无拖拽上传** — 必须点击按钮选择文件，不支持拖拽
3. **无搜索/筛选** — 素材多了后无法按名称、类型、时长筛选
4. **无批量操作** — 每次只能操作一个素材
5. **无上传进度** — 上传后只能等 loading 消失，无进度条
6. **素材管理弹窗层级过深** — 打开弹窗 → 再打开子弹窗，交互路径长

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **网格/列表双视图** — 顶部切换按钮支持「网格视图」（缩略图卡片）和「列表视图」（详细表格） | P1 |
| **缩略图预览** — 网格视图下每个素材显示视频首帧缩略图 + 时长角标 + hover 播放预览 | P1 |
| **拖拽上传区域** — 空状态和素材列表都支持拖拽文件上传，拖入时高亮边框和提示文字 | P1 |
| **搜索栏** — 顶部添加搜索输入框，支持按名称和描述模糊搜索 | P1 |
| **筛选标签** — 时长筛选（`<30s`、`30s-1min`、`>1min`）+ 类型标签（已使用/未使用） | P2 |
| **批量选择** — 复选框 + 批量操作栏（批量添加到项目/批量删除） | P2 |
| **上传进度条** — 文件上传时显示进度百分比，多文件时显示队列 | P1 |
| **素材详情侧边栏** — 点击素材在右侧滑出详情面板（大缩略图、完整描述、分段信息、操作历史），替代弹窗 | P2 |
| **空状态优化** — 无素材时显示更友好的引导：拖拽上传区域 + 获取素材引导步骤 | P2 |

---

## 七、视频预览（PreviewPanel）

### 现状问题
1. **无渲染进度** — 渲染时仅按钮 loading，无百分比进度
2. **时间轴过于简单** — 14px 高度的色条，无法交互
3. **视频列表简陋** — 纯文字列表，无缩略图
4. **无帧导航** — 没有逐帧前进/后退能力
5. **无渲染参数** — 不能选择输出质量、分辨率、格式

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **渲染进度条** — 渲染时显示模态进度面板（百分比 + 预估剩余时间 + 取消按钮） | P1 |
| **可交互时间轴** — 时间轴支持：点击跳转、hover 预览帧、当前播放位置指示器 | P2 |
| **视频缩略图列表** — 生成记录每项显示首帧缩略图，选中项高亮边框 | P2 |
| **渲染参数面板** — 渲染前弹出参数选择：分辨率（1080p/720p）、格式（MP4/GIF）、帧率（30/60） | P2 |
| **快捷键控制** — 空格暂停/播放，左右键快进/快退 5s，`[` `]` 逐帧 | P3 |
| **全屏预览** — 双击视频或按钮进入全屏预览模式 | P2 |
| **对比视图** — 渲染新版本后支持与旧版本左右对比播放 | P3 |

---

## 八、全局交互增强

### 缺失的全局能力

| 能力 | 说明 | 优先级 |
|------|------|--------|
| **键盘快捷键体系** | 建立「命令面板」（`Ctrl+Shift+P`），支持搜索执行任意命令，参考 VS Code | P1 |
| **操作反馈 Toast** | 统一 Toast 样式和位置（右上角），成功用绿色、警告用黄色、错误用红色，带图标和进度条 | P2 |
| **全局加载指示** | 页面顶部的全局加载条（类似 NProgress），所有 API 请求自动触发 | P1 |
| **右键菜单** | 素材、片段、消息等元素支持右键上下文菜单 | P2 |
| **暗色主题** | 支持 Light/Dark 主题切换，利用 Element Plus 的 CSS 变量机制 | P3 |
| **操作确认优化** | 危险操作（删除、覆盖）使用内联确认（输入确认文字），替代简单的 MessageBox | P2 |
| **表单自动保存提示** | 编辑中的表单（项目名、描述等）在离开前检测未保存变更，弹出保留/放弃提示 | P1 |
| **响应式状态提示** | API 连接断开时在底部显示重连提示条，WebSocket 断开时显示通知 | P2 |

---

## 九、动效与微交互

### 现状问题
- 折叠/展开仅 CSS transition，缺少弹性动画
- 按钮无 press/hover 动效
- 列表增删无过渡动画
- 状态切换无视觉引导

### 优化建议

| 场景 | 建议 | 优先级 |
|------|------|--------|
| **面板折叠/展开** | 使用 `cubic-bezier(0.4, 0, 0.2, 1)` 曲线 + 微弹性，折叠时面板内容渐隐（opacity） | P2 |
| **按钮交互** | hover 时 `transform: translateY(-1px)` + shadow 增强，active 时 `scale(0.97)` | P2 |
| **列表动画** | 使用 `<TransitionGroup>` 实现列表增删的滑入/滑出动画 | P2 |
| **Tab 切换** | 内容区切换时使用 fade + slide 微动效 | P3 |
| **Skeleton 加载** | 数据加载时使用骨架屏（Skeleton）替代简单的 loading 文字/转圈 | P1 |
| **空状态动画** | 空状态插图添加微动效（如轻微呼吸动画），增强生动感 | P3 |
| **工具调用可视化** | AI 调用工具时，在对话中展示工具卡片（图标 + 名称 + 参数摘要 + 状态指示），替代纯文本 | P1 |
| **渲染完成通知** | 渲染完成后发送系统通知（Notification API）+ 声音提示，即使窗口不在前台 | P2 |

---

## 十、移动端/窄屏适配

### 现状问题
- `<900px` 仅简单堆叠，不适合实际移动端使用
- 所有面板同时显示，信息过载

### 优化建议

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **底部 Tab 导航** | 移动端底部显示 3 个 Tab：工作区、对话、素材预览 | P2 |
| **手势支持** | 左右滑动切换面板，下拉刷新素材 | P3 |
| **浮动 AI 按钮** | 在非对话 Tab 时显示浮动 AI 入口按钮，点击弹出对话抽屉 | P3 |

---

## 优先级汇总

### P1（核心体验，建议优先实施）
1. 面板可拖拽分割条（替代固定比例）
2. 片段时间轴/故事板可视化视图
3. 素材网格视图 + 缩略图 + 拖拽上传
4. AI 对话自适应输入框 + 确认面板优化 + 流式打字效果
5. 全局键盘快捷键 + 命令面板
6. 全局加载进度条 + 渲染进度面板
7. Skeleton 骨架屏加载态
8. 工具调用可视化卡片
9. 面板状态 localStorage 持久化
10. 表单未保存离开提示

### P2（体验提升，中期实施）
1. 项目卡片视图（替代表格）
2. 工具菜单分组 + 快捷键标注
3. 布局预设切换
4. 片段拖拽排序 + 缩略图
5. 消息时间戳 + 操作菜单 + 打字指示器
6. 素材搜索/筛选 + 批量操作 + 上传进度
7. 视频缩略图列表 + 可交互时间轴
8. 右键上下文菜单
9. 操作确认优化（内联确认）
10. 动效与微交互（按钮/面板/列表）
11. 渲染完成通知

### P3（锦上添花，长期规划）
1. 品牌视觉增强 + 新手引导
2. 全局搜索
3. 暗色主题
4. 消息搜索
5. 对话模板
6. 移动端适配
7. 视频对比视图
8. 全屏预览

---

## 技术实施建议

### 依赖引入
- **拖拽分割条**: `splitpanes` 或自定义实现（~100 行）
- **拖拽排序**: `vuedraggable` (基于 SortableJS)
- **时间轴**: 自定义 Canvas 或 SVG 组件（参考 `wavesurfer.js` 思路）
- **骨架屏**: Element Plus `<el-skeleton>` 原生支持
- **快捷键**: 自定义 composable `useHotkeys()` + `mousetrap` 或直接监听 keydown
- **命令面板**: 自定义 `<CommandPalette>` 组件，参考 `cmdk` (React) 实现
- **通知**: 浏览器原生 `Notification API` + `el-notification`

### 实施策略
1. **渐进式改造** — 优先改 P1 项，每项独立可交付，不阻塞现有功能
2. **组件化抽取** — 通用交互模式（拖拽分割、骨架屏、右键菜单）抽取为独立组件
3. **CSS 变量驱动** — 所有颜色、间距、圆角使用 CSS 变量，为暗色主题打基础
4. **性能预算** — 新增依赖控制在 gzip 后 < 50KB，动效保持 60fps
